# Python dependencies for the Insight Harbor transform layer
# Run: pip install -r transform/requirements.txt

# Azure Data Lake Storage Gen2 access
azure-storage-blob>=12.0.0

# Optional: ~5-10x faster JSON parsing in explosion processor
# orjson>=3.9.0
